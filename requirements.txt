# Default environment for the legacy TRADE_ORACLE trading pipeline.
#
# This file intentionally excludes the LangGraph / FastAPI orchestration stack
# because those packages pull newer httpx/websockets versions that conflict with
# the existing Supabase-based trading environment.
#
# For the LangGraph + MCP architecture, use:
#   requirements.langgraph.txt

-r requirements.trading.txt
